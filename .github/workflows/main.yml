name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "Workflow is running on the main branch"
          else
            echo "Workflow is not running on the main branch"
          fi
       
